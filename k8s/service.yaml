apiVersion: v1
kind: Service
metadata:
  name: productapi-service
  labels:
    app: productapi
spec:
  type: LoadBalancer
  selector:
    app: productapi
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080